# SocialMedia_DocParser

## Setup

1. Install Python dependencies: `pip install -r requirements.txt`
2. Install Tesseract: https://github.com/UB-Mannheim/tesseract/wiki (Windows) and make sure it's added to PATH.
3. Run the app: `uvicorn backend.app:app --reload`
