# DocIntell

Document upload, PDF-to-image conversion, and OCR pipeline.

## Structure

- `backend/` — FastAPI app (routes, services, utils)
- `frontend/` — static UI (index.html, style.css, main.js)

## Running

```
pip install -r requirements.txt
cd backend
uvicorn app:app --reload
```
